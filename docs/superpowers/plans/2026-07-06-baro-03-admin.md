# baro Plan 3/6 — 관리자 대시보드 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Better Auth admin 플러그인 기반 `/admin` 대시보드 — 유저 목록/검색/정지/역할 변경 + 기본 통계(총 가입자, 14일 가입 추이 차트) + 첫 어드민 seed 스크립트.

**Architecture:** packages/auth에 admin 플러그인 추가(스키마 필드는 Plan 2에서 선반영 완료 — 마이그레이션 diff 0이어야 함). api에 `requireAdmin` 미들웨어 + `/api/admin/stats` RPC 라우트. web은 adminClient 플러그인으로 유저 관리(Better Auth 내장 엔드포인트), stats는 RPC. Base UI 프리미티브가 여기서 처음 도입(Dialog=정지 확인, Select=역할 변경) — shadcn 방식 래퍼로 코드 소유.

**Tech Stack:** better-auth admin plugin(server)/adminClient(web), @base-ui-components/react, drizzle 집계 쿼리, 수제 SVG 바 차트(의존성 0)

**참조 스펙:** `docs/superpowers/specs/2026-07-05-baro-fullstack-design.md` §4.2
**선행:** Plan 2 완료 (main, CI 그린)

## Global Constraints

- 패키지 네이밍 `@baro/<이름>`, `"private": true`. 포트 web 3000 / api 8000
- 코드젠 금지(타입=TS 추론). 내부 패키지 TS 소스 export. Biome 하나만. Conventional Commits
- `apps/api/src/app.ts` 체인 순서 주석 준수 — `.route()` 마지막 유지
- 에러 포맷 통일(스펙 §6): `{ error: { code, message } }`
- lockfile 규칙: 의존성 변경 시 `pnpm install` + `git status --short` 증거 + lockfile 동일 커밋
- rtk 환경 주의: pnpm exit 254 시 `rtk proxy pnpm <동일 인자>` 재시도
- 브라우저 확인은 `127.0.0.1:3000` (localhost:3000 포트 점유자 존재), 단 쿠키는 same-site 필요 — 브라우저 테스트 시 `NEXT_PUBLIC_API_URL=http://127.0.0.1:8000`로 web 기동
- 스키마 회귀 금지: admin 플러그인 추가 후 `drizzle-kit generate`가 **새 마이그레이션을 만들지 않아야 정상** (필드 선반영 검증). 새 파일이 생기면 원인 조사 후 보고

---

### Task 1: admin 플러그인 + seed 스크립트

**Files:**
- Modify: `packages/auth/src/index.ts` (admin 플러그인 추가)
- Modify: `packages/auth/src/auth.test.ts` (admin 테스트 추가)
- Create: `apps/api/src/scripts/seed-admin.ts`
- Modify: `apps/api/package.json` (`seed:admin` 스크립트)
- Modify: `package.json` 루트 (`seed:admin` 위임 스크립트)

**Interfaces:**
- Consumes: `createAuth(db)` (Plan 2), `schema.user` admin 필드(role/banned/banReason/banExpires — 이미 존재)
- Produces:
  - `createAuth`가 admin 플러그인 포함 — `auth.api.listUsers`, `banUser`, `unbanUser`, `setRole` 등 서버 API + `/api/auth/admin/*` HTTP 엔드포인트 활성
  - `pnpm seed:admin <email> <password>` — 첫 어드민 생성 (이미 존재하면 role만 승격)

- [ ] **Step 1: 실패하는 테스트 추가**

`packages/auth/src/auth.test.ts`에 추가 (기존 describe 안, 기존 beforeAll의 `auth` 재사용):

```ts
it("admin can list users via admin plugin", async () => {
  await auth.api.signUpEmail({
    body: { name: "Admin", email: "admin@example.com", password: "password1234" },
  });
  // 직접 role 승격 (seed 스크립트와 동일 방식)
  const { eq } = await import("drizzle-orm");
  const { schema } = await import("@baro/db");
  await testDb
    .update(schema.user)
    .set({ role: "admin" })
    .where(eq(schema.user.email, "admin@example.com"));

  const signIn = await auth.api.signInEmail({
    body: { email: "admin@example.com", password: "password1234" },
    asResponse: true,
  });
  const cookie = signIn.headers.get("set-cookie") ?? "";

  const users = await auth.api.listUsers({
    query: { limit: 100 },
    headers: new Headers({ cookie }),
  });
  expect(users.users.length).toBeGreaterThanOrEqual(1);
});

it("non-admin cannot list users", async () => {
  await auth.api.signUpEmail({
    body: { name: "Pleb", email: "pleb@example.com", password: "password1234" },
  });
  const signIn = await auth.api.signInEmail({
    body: { email: "pleb@example.com", password: "password1234" },
    asResponse: true,
  });
  const cookie = signIn.headers.get("set-cookie") ?? "";

  await expect(
    auth.api.listUsers({
      query: { limit: 100 },
      headers: new Headers({ cookie }),
    }),
  ).rejects.toThrow();
});
```

주의: 기존 beforeAll의 `db` 변수를 테스트에서 접근할 수 있게 `testDb`로 노출 필요 — describe 스코프 변수로 리팩터 (`let testDb: Db`). 기존 테스트 깨지지 않게 유지.

- [ ] **Step 2: 테스트 실패 확인**

Run: `rtk proxy pnpm --filter @baro/auth test`
Expected: FAIL — `listUsers` 미존재 (admin 플러그인 없음)

- [ ] **Step 3: admin 플러그인 추가**

`packages/auth/src/index.ts`:

```ts
import { admin } from "better-auth/plugins";
```

`betterAuth({ ... })` 설정에 추가:

```ts
plugins: [admin()],
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `rtk proxy pnpm --filter @baro/auth test`
Expected: PASS — 기존 3 + 신규 2 = 5 passed

- [ ] **Step 5: 스키마 회귀 검증**

Run: `rtk proxy pnpm --filter @baro/db db:generate`
Expected: "No schema changes" 류 메시지, `packages/db/drizzle/`에 새 파일 없음 (`git status --short`로 확인). 새 마이그레이션이 생기면 admin 플러그인 요구 필드와 기존 스키마의 차이를 조사·보고.

- [ ] **Step 6: seed 스크립트 작성**

`apps/api/src/scripts/seed-admin.ts`:

```ts
import { createAuth } from "@baro/auth";
import { getDb, runMigrations, schema } from "@baro/db";
import { eq } from "drizzle-orm";

const email = process.env.ADMIN_EMAIL ?? process.argv[2];
const password = process.env.ADMIN_PASSWORD ?? process.argv[3];

if (!email || !password) {
  console.error("usage: pnpm seed:admin <email> <password>");
  process.exit(1);
}

const db = getDb();
await runMigrations(db);
const auth = createAuth(db);

const existing = await db
  .select()
  .from(schema.user)
  .where(eq(schema.user.email, email));

if (existing.length === 0) {
  await auth.api.signUpEmail({
    body: { name: "Admin", email, password },
  });
}

await db
  .update(schema.user)
  .set({ role: "admin" })
  .where(eq(schema.user.email, email));

console.log(`[baro] admin ready: ${email}`);
process.exit(0);
```

`apps/api/package.json` scripts에 추가:

```json
"seed:admin": "tsx src/scripts/seed-admin.ts"
```

루트 `package.json` scripts에 추가:

```json
"seed:admin": "pnpm --filter @baro/api seed:admin"
```

- [ ] **Step 7: seed 수동 검증**

Run: `rtk proxy pnpm seed:admin seed-test@example.com password1234` (임시 PGLITE_DATA_DIR로: `PGLITE_DATA_DIR=/tmp/baro-seed-test`)
Expected: `[baro] admin ready: seed-test@example.com`. 재실행해도 성공(멱등). `/tmp/baro-seed-test` 삭제.

- [ ] **Step 8: Commit**

```bash
git add packages/auth apps/api package.json pnpm-lock.yaml
git commit -m "feat(auth): enable admin plugin and add seed-admin script"
```

---

### Task 2: api admin 모듈 — requireAdmin + stats

**Files:**
- Create: `packages/shared/src/schemas/admin.ts`
- Create: `apps/api/src/modules/admin/middleware.ts`
- Create: `apps/api/src/modules/admin/routes.ts`
- Modify: `apps/api/src/app.ts` (admin 라우트 마운트)
- Test: `apps/api/src/modules/admin/routes.test.ts`

**Interfaces:**
- Consumes: `auth`(modules/auth/routes.ts의 인스턴스), `getDb`, `schema.user`
- Produces:
  - `GET /api/admin/stats` → `{ totalUsers: number, signupsByDay: [{ day: "YYYY-MM-DD", count: number }] }` (14일, RPC 타입 전파)
  - 401(비로그인)/403(비어드민) — 에러 포맷 `{ error: { code, message } }`
  - `requireAdmin` 미들웨어 — 이후 admin 라우트 공용

- [ ] **Step 1: shared 스키마**

`packages/shared/src/schemas/admin.ts`:

```ts
import { z } from "zod";

export const adminStatsSchema = z.object({
  totalUsers: z.number(),
  signupsByDay: z.array(
    z.object({
      day: z.string(), // YYYY-MM-DD
      count: z.number(),
    }),
  ),
});

export type AdminStats = z.infer<typeof adminStatsSchema>;
```

- [ ] **Step 2: 실패하는 테스트 작성**

`apps/api/src/modules/admin/routes.test.ts`:

```ts
import { describe, expect, it } from "vitest";
import { app } from "../../app";

async function signUpAndGetCookie(email: string): Promise<string> {
  const res = await app.request("/api/auth/sign-up/email", {
    method: "POST",
    headers: { "content-type": "application/json" },
    body: JSON.stringify({ name: "T", email, password: "password1234" }),
  });
  return res.headers.get("set-cookie") ?? "";
}

describe("GET /api/admin/stats", () => {
  it("401 without a session", async () => {
    const res = await app.request("/api/admin/stats");
    expect(res.status).toBe(401);
    const body = await res.json();
    expect(body.error.code).toBe("UNAUTHORIZED");
  });

  it("403 for a non-admin user", async () => {
    const cookie = await signUpAndGetCookie("stats-pleb@example.com");
    const res = await app.request("/api/admin/stats", {
      headers: { cookie },
    });
    expect(res.status).toBe(403);
    const body = await res.json();
    expect(body.error.code).toBe("FORBIDDEN");
  });

  it("200 with counts for an admin", async () => {
    const cookie = await signUpAndGetCookie("stats-admin@example.com");
    const { getDb, schema } = await import("@baro/db");
    const { eq } = await import("drizzle-orm");
    await getDb()
      .update(schema.user)
      .set({ role: "admin" })
      .where(eq(schema.user.email, "stats-admin@example.com"));

    const res = await app.request("/api/admin/stats", {
      headers: { cookie },
    });
    expect(res.status).toBe(200);
    const body = await res.json();
    expect(body.totalUsers).toBeGreaterThanOrEqual(2);
    expect(Array.isArray(body.signupsByDay)).toBe(true);
    expect(body.signupsByDay.length).toBeGreaterThanOrEqual(1);
    expect(body.signupsByDay[0]).toHaveProperty("day");
    expect(body.signupsByDay[0]).toHaveProperty("count");
  });
});
```

주의: role 변경 후에도 기존 세션이 이전 role을 캐시할 수 있음(Better Auth 세션에 user 스냅샷). 403이 나오면 role 승격 **후** 재로그인(sign-in 재호출로 새 쿠키 획득)하도록 테스트 조정 — 실제 동작에 맞추고 보고서에 기록.

- [ ] **Step 3: 테스트 실패 확인**

Run: `rtk proxy pnpm --filter @baro/api test`
Expected: admin 테스트 3개 FAIL (404 — 라우트 없음)

- [ ] **Step 4: 미들웨어 구현**

`apps/api/src/modules/admin/middleware.ts`:

```ts
import { createMiddleware } from "hono/factory";
import { auth } from "../auth/routes";

type SessionUser = NonNullable<
  Awaited<ReturnType<typeof auth.api.getSession>>
>["user"];

export type AdminEnv = {
  Variables: {
    adminUser: SessionUser;
  };
};

export const requireAdmin = createMiddleware<AdminEnv>(async (c, next) => {
  const session = await auth.api.getSession({
    headers: c.req.raw.headers,
  });
  if (!session) {
    return c.json(
      { error: { code: "UNAUTHORIZED", message: "Sign in required" } },
      401,
    );
  }
  if (session.user.role !== "admin") {
    return c.json(
      { error: { code: "FORBIDDEN", message: "Admin access required" } },
      403,
    );
  }
  c.set("adminUser", session.user);
  await next();
});
```

- [ ] **Step 5: stats 라우트 구현**

`apps/api/src/modules/admin/routes.ts`:

```ts
import { getDb, schema } from "@baro/db";
import { adminStatsSchema } from "@baro/shared/schemas/admin";
import { createRoute, OpenAPIHono } from "@hono/zod-openapi";
import { count, gte, sql } from "drizzle-orm";
import { type AdminEnv, requireAdmin } from "./middleware";

const statsRoute = createRoute({
  method: "get",
  path: "/stats",
  responses: {
    200: {
      description: "Admin dashboard stats",
      content: { "application/json": { schema: adminStatsSchema } },
    },
  },
});

export const adminRoutes = new OpenAPIHono<AdminEnv>()
  .use("/*", requireAdmin)
  .openapi(statsRoute, async (c) => {
    const db = getDb();
    const [total] = await db
      .select({ value: count() })
      .from(schema.user);

    const since = new Date(Date.now() - 13 * 24 * 60 * 60 * 1000);
    since.setHours(0, 0, 0, 0);

    const rows = await db
      .select({
        day: sql<string>`to_char(${schema.user.createdAt}, 'YYYY-MM-DD')`,
        count: count(),
      })
      .from(schema.user)
      .where(gte(schema.user.createdAt, since))
      .groupBy(sql`to_char(${schema.user.createdAt}, 'YYYY-MM-DD')`)
      .orderBy(sql`to_char(${schema.user.createdAt}, 'YYYY-MM-DD')`);

    return c.json(
      {
        totalUsers: total.value,
        signupsByDay: rows.map((r) => ({ day: r.day, count: Number(r.count) })),
      },
      200,
    );
  });
```

- [ ] **Step 6: app.ts 마운트**

`apps/api/src/app.ts` — 체인 마지막 `.route("/api/auth", authRoutes)` 뒤에 이어서:

```ts
.route("/api/admin", adminRoutes)
```

(import 추가: `import { adminRoutes } from "./modules/admin/routes";`. 체인 순서 주석 유지.)

- [ ] **Step 7: 테스트 통과 확인**

Run: `rtk proxy pnpm --filter @baro/api test && rtk proxy pnpm --filter @baro/api typecheck && rtk proxy pnpm --filter @baro/web typecheck`
Expected: api 7 passed (기존 4 + 신규 3), 양쪽 typecheck 클린 (AppType 체인 무결)

- [ ] **Step 8: Commit**

```bash
git add apps/api packages/shared
git commit -m "feat(api): add admin stats route with requireAdmin middleware"
```

---

### Task 3: web /admin — 대시보드 UI

**Files:**
- Modify: `apps/web/package.json` (`@base-ui-components/react` 추가)
- Modify: `apps/web/src/lib/auth-client.ts` (adminClient 플러그인)
- Modify: `apps/web/src/lib/api.ts` (credentials include)
- Create: `apps/web/src/components/ui/dialog.tsx` (Base UI 래퍼)
- Create: `apps/web/src/components/ui/select.tsx` (Base UI 래퍼)
- Create: `apps/web/src/features/admin/stats-cards.tsx`
- Create: `apps/web/src/features/admin/signup-chart.tsx`
- Create: `apps/web/src/features/admin/user-table.tsx`
- Create: `apps/web/src/app/admin/page.tsx`
- Modify: `apps/web/src/features/auth/session-widget.tsx` (admin이면 /admin 링크)

**Interfaces:**
- Consumes: `GET /api/admin/stats` RPC (Task 2), `authClient.admin.*` (Task 1 플러그인의 HTTP 엔드포인트)
- Produces: `/admin` 페이지 — 클라이언트 가드(비어드민 리다이렉트) + 통계 카드 + 14일 가입 차트 + 유저 테이블(검색/정지/해제/역할 변경). 보안 집행은 api(401/403) — 클라이언트 가드는 UX용

- [ ] **Step 1: 의존성 + 클라이언트 플러그인**

`apps/web/package.json` dependencies에 `"@base-ui-components/react": "^1.0.0"` 추가 (해석 실패 시 최신 호환 버전, 보고).

`apps/web/src/lib/auth-client.ts`:

```ts
import { adminClient } from "better-auth/client/plugins";
import { createAuthClient } from "better-auth/react";

export const authClient = createAuthClient({
  baseURL: `${process.env.NEXT_PUBLIC_API_URL ?? "http://localhost:8000"}/api/auth`,
  plugins: [adminClient()],
});

export const { signIn, signUp, signOut, useSession } = authClient;
```

`apps/web/src/lib/api.ts` — RPC 호출에 쿠키 포함:

```ts
import type { AppType } from "@baro/api/client";
import { hc } from "hono/client";

export const api = hc<AppType>(
  process.env.NEXT_PUBLIC_API_URL ?? "http://localhost:8000",
  { init: { credentials: "include" } },
);
```

- [ ] **Step 2: Base UI 래퍼 2개**

`apps/web/src/components/ui/dialog.tsx`:

```tsx
"use client";

import { Dialog as BaseDialog } from "@base-ui-components/react/dialog";
import type { ReactNode } from "react";

export function ConfirmDialog({
  trigger,
  title,
  description,
  confirmLabel,
  onConfirm,
}: {
  trigger: ReactNode;
  title: string;
  description: string;
  confirmLabel: string;
  onConfirm: () => void;
}) {
  return (
    <BaseDialog.Root>
      <BaseDialog.Trigger
        render={(props) => <span {...props}>{trigger}</span>}
      />
      <BaseDialog.Portal>
        <BaseDialog.Backdrop className="fixed inset-0 bg-black/40" />
        <BaseDialog.Popup className="fixed left-1/2 top-1/2 w-full max-w-sm -translate-x-1/2 -translate-y-1/2 rounded-xl border border-zinc-200 bg-white p-6 shadow-lg dark:border-zinc-800 dark:bg-zinc-950">
          <BaseDialog.Title className="text-lg font-semibold">
            {title}
          </BaseDialog.Title>
          <BaseDialog.Description className="mt-2 text-sm text-zinc-500">
            {description}
          </BaseDialog.Description>
          <div className="mt-4 flex justify-end gap-2">
            <BaseDialog.Close className="inline-flex h-9 items-center rounded-md border border-zinc-300 px-4 text-sm hover:bg-zinc-100 dark:border-zinc-700 dark:hover:bg-zinc-800">
              Cancel
            </BaseDialog.Close>
            <BaseDialog.Close
              className="inline-flex h-9 items-center rounded-md bg-red-600 px-4 text-sm font-medium text-white hover:bg-red-700"
              onClick={onConfirm}
            >
              {confirmLabel}
            </BaseDialog.Close>
          </div>
        </BaseDialog.Popup>
      </BaseDialog.Portal>
    </BaseDialog.Root>
  );
}
```

`apps/web/src/components/ui/select.tsx`:

```tsx
"use client";

import { Select as BaseSelect } from "@base-ui-components/react/select";

export function Select({
  value,
  options,
  onValueChange,
  ariaLabel,
}: {
  value: string;
  options: { value: string; label: string }[];
  onValueChange: (value: string) => void;
  ariaLabel: string;
}) {
  return (
    <BaseSelect.Root value={value} onValueChange={onValueChange}>
      <BaseSelect.Trigger
        aria-label={ariaLabel}
        className="inline-flex h-8 items-center gap-1 rounded-md border border-zinc-300 px-2 text-sm dark:border-zinc-700"
      >
        <BaseSelect.Value />
        <BaseSelect.Icon className="text-zinc-500">▾</BaseSelect.Icon>
      </BaseSelect.Trigger>
      <BaseSelect.Portal>
        <BaseSelect.Positioner sideOffset={4}>
          <BaseSelect.Popup className="rounded-md border border-zinc-200 bg-white py-1 shadow-md dark:border-zinc-800 dark:bg-zinc-950">
            {options.map((o) => (
              <BaseSelect.Item
                key={o.value}
                value={o.value}
                className="cursor-default px-3 py-1.5 text-sm data-[highlighted]:bg-zinc-100 dark:data-[highlighted]:bg-zinc-800"
              >
                <BaseSelect.ItemText>{o.label}</BaseSelect.ItemText>
              </BaseSelect.Item>
            ))}
          </BaseSelect.Popup>
        </BaseSelect.Positioner>
      </BaseSelect.Portal>
    </BaseSelect.Root>
  );
}
```

주의: Base UI 1.x의 실제 컴포넌트 API(`render` prop, `data-[highlighted]` 등)는 설치 버전 문서에 맞춰 조정 가능 — 동작(접근성 있는 다이얼로그/셀렉트)이 요구사항, 세부 prop은 편차 허용·보고.

- [ ] **Step 3: 통계 카드 + 차트**

`apps/web/src/features/admin/stats-cards.tsx`:

```tsx
import { Card } from "@/components/ui/card";
import type { AdminStats } from "@baro/shared/schemas/admin";

export function StatsCards({ stats }: { stats: AdminStats }) {
  const last7 = stats.signupsByDay.slice(-7).reduce((s, d) => s + d.count, 0);
  return (
    <div className="grid grid-cols-1 gap-4 sm:grid-cols-2">
      <Card>
        <p className="text-sm text-zinc-500">Total users</p>
        <p className="mt-1 text-3xl font-semibold tabular-nums">
          {stats.totalUsers}
        </p>
      </Card>
      <Card>
        <p className="text-sm text-zinc-500">Signups (last 7 days)</p>
        <p className="mt-1 text-3xl font-semibold tabular-nums">{last7}</p>
      </Card>
    </div>
  );
}
```

`apps/web/src/features/admin/signup-chart.tsx` — 단일 시리즈 SVG 바 차트 (범례 불필요 — 제목이 시리즈 명명, 값 라벨은 최대·마지막만, 바 간격 2px, 텍스트는 잉크 토큰):

```tsx
import { Card } from "@/components/ui/card";
import type { AdminStats } from "@baro/shared/schemas/admin";

const W = 560;
const H = 120;
const GAP = 2;

export function SignupChart({ stats }: { stats: AdminStats }) {
  // 14일 연속 축 구성 (누락일 0 채움)
  const byDay = new Map(stats.signupsByDay.map((d) => [d.day, d.count]));
  const days: { day: string; count: number }[] = [];
  const now = new Date();
  for (let i = 13; i >= 0; i--) {
    const d = new Date(now);
    d.setDate(d.getDate() - i);
    const key = d.toISOString().slice(0, 10);
    days.push({ day: key, count: byDay.get(key) ?? 0 });
  }

  const max = Math.max(1, ...days.map((d) => d.count));
  const maxIdx = days.findIndex((d) => d.count === max);
  const barW = (W - GAP * (days.length - 1)) / days.length;

  return (
    <Card>
      <p className="text-sm text-zinc-500">Signups — last 14 days</p>
      <svg
        viewBox={`0 0 ${W} ${H + 20}`}
        className="mt-2 w-full"
        role="img"
        aria-label={`Daily signups for the last 14 days, peak ${max}`}
      >
        {days.map((d, i) => {
          const h = Math.max(2, (d.count / max) * H);
          const x = i * (barW + GAP);
          const isLabeled = i === maxIdx || i === days.length - 1;
          return (
            <g key={d.day}>
              <rect
                x={x}
                y={H - h}
                width={barW}
                height={h}
                rx={3}
                className="fill-zinc-800 dark:fill-zinc-200"
              >
                <title>{`${d.day}: ${d.count}`}</title>
              </rect>
              {isLabeled && d.count > 0 && (
                <text
                  x={x + barW / 2}
                  y={H - h - 4}
                  textAnchor="middle"
                  className="fill-zinc-500 text-[10px] tabular-nums"
                >
                  {d.count}
                </text>
              )}
              {(i === 0 || i === days.length - 1) && (
                <text
                  x={x + barW / 2}
                  y={H + 14}
                  textAnchor="middle"
                  className="fill-zinc-400 text-[10px]"
                >
                  {d.day.slice(5)}
                </text>
              )}
            </g>
          );
        })}
      </svg>
    </Card>
  );
}
```

- [ ] **Step 4: 유저 테이블**

`apps/web/src/features/admin/user-table.tsx`:

```tsx
"use client";

import { Button } from "@/components/ui/button";
import { ConfirmDialog } from "@/components/ui/dialog";
import { Input } from "@/components/ui/input";
import { Select } from "@/components/ui/select";
import { authClient } from "@/lib/auth-client";
import { useCallback, useEffect, useState } from "react";

type AdminUser = {
  id: string;
  name: string;
  email: string;
  role?: string | null;
  banned?: boolean | null;
  createdAt: Date | string;
};

export function UserTable() {
  const [users, setUsers] = useState<AdminUser[]>([]);
  const [search, setSearch] = useState("");
  const [error, setError] = useState<string | null>(null);

  const load = useCallback(async () => {
    const res = await authClient.admin.listUsers({
      query: {
        limit: 50,
        ...(search && {
          searchField: "email",
          searchOperator: "contains",
          searchValue: search,
        }),
      },
    });
    if (res.error) {
      setError(res.error.message ?? "Failed to load users");
      return;
    }
    setUsers(res.data.users as AdminUser[]);
  }, [search]);

  useEffect(() => {
    load();
  }, [load]);

  async function setRole(userId: string, role: "user" | "admin") {
    await authClient.admin.setRole({ userId, role });
    load();
  }

  async function toggleBan(u: AdminUser) {
    if (u.banned) {
      await authClient.admin.unbanUser({ userId: u.id });
    } else {
      await authClient.admin.banUser({ userId: u.id });
    }
    load();
  }

  return (
    <div className="flex flex-col gap-3">
      <Input
        placeholder="Search by email…"
        value={search}
        onChange={(e) => setSearch(e.target.value)}
        aria-label="Search users by email"
      />
      {error && <p className="text-sm text-red-600">{error}</p>}
      <div className="overflow-x-auto rounded-xl border border-zinc-200 dark:border-zinc-800">
        <table className="w-full text-sm">
          <thead>
            <tr className="border-b border-zinc-200 text-left text-zinc-500 dark:border-zinc-800">
              <th className="p-3 font-medium">Email</th>
              <th className="p-3 font-medium">Name</th>
              <th className="p-3 font-medium">Role</th>
              <th className="p-3 font-medium">Status</th>
              <th className="p-3 font-medium">Actions</th>
            </tr>
          </thead>
          <tbody>
            {users.map((u) => (
              <tr
                key={u.id}
                className="border-b border-zinc-100 last:border-0 dark:border-zinc-900"
              >
                <td className="p-3">{u.email}</td>
                <td className="p-3">{u.name}</td>
                <td className="p-3">
                  <Select
                    value={u.role ?? "user"}
                    options={[
                      { value: "user", label: "user" },
                      { value: "admin", label: "admin" },
                    ]}
                    onValueChange={(v) => setRole(u.id, v as "user" | "admin")}
                    ariaLabel={`Role for ${u.email}`}
                  />
                </td>
                <td className="p-3">
                  {u.banned ? (
                    <span className="text-red-600">banned</span>
                  ) : (
                    <span className="text-zinc-500">active</span>
                  )}
                </td>
                <td className="p-3">
                  <ConfirmDialog
                    trigger={
                      <Button variant="outline">
                        {u.banned ? "Unban" : "Ban"}
                      </Button>
                    }
                    title={u.banned ? "Unban user" : "Ban user"}
                    description={`${u.email} — ${
                      u.banned
                        ? "restore access for this user?"
                        : "this user will no longer be able to sign in."
                    }`}
                    confirmLabel={u.banned ? "Unban" : "Ban"}
                    onConfirm={() => toggleBan(u)}
                  />
                </td>
              </tr>
            ))}
          </tbody>
        </table>
      </div>
    </div>
  );
}
```

- [ ] **Step 5: /admin 페이지 + 세션 위젯 링크**

`apps/web/src/app/admin/page.tsx`:

```tsx
"use client";

import { SignupChart } from "@/features/admin/signup-chart";
import { StatsCards } from "@/features/admin/stats-cards";
import { UserTable } from "@/features/admin/user-table";
import { api } from "@/lib/api";
import { useSession } from "@/lib/auth-client";
import type { AdminStats } from "@baro/shared/schemas/admin";
import { useRouter } from "next/navigation";
import { useEffect, useState } from "react";

export default function AdminPage() {
  const router = useRouter();
  const { data: session, isPending } = useSession();
  const [stats, setStats] = useState<AdminStats | null>(null);

  const isAdmin = session?.user.role === "admin";

  useEffect(() => {
    if (isPending) return;
    if (!isAdmin) {
      router.replace("/");
      return;
    }
    api.api.admin.stats
      .$get()
      .then((res) => (res.ok ? res.json() : null))
      .then(setStats)
      .catch(() => setStats(null));
  }, [isPending, isAdmin, router]);

  if (isPending || !isAdmin) return null;

  return (
    <main className="mx-auto flex max-w-4xl flex-col gap-6 p-6">
      <h1 className="text-xl font-semibold">Admin</h1>
      {stats && (
        <>
          <StatsCards stats={stats} />
          <SignupChart stats={stats} />
        </>
      )}
      <UserTable />
    </main>
  );
}
```

`apps/web/src/features/auth/session-widget.tsx` — 로그인 상태 분기에 admin 링크 추가 (session 있을 때 렌더 블록):

```tsx
{session.user.role === "admin" && (
  <Link href="/admin">
    <Button variant="ghost">Admin</Button>
  </Link>
)}
```

(주의: `useSession`의 user 타입에 role이 없으면 adminClient 플러그인이 타입 확장하는지 확인 — 안 되면 `session.user as { role?: string }` 단언 + 보고.)

- [ ] **Step 6: 검증**

Run: `rtk proxy pnpm --filter @baro/web typecheck && rtk proxy pnpm check`
Expected: 클린.

수동 플로우: `PGLITE_DATA_DIR=/tmp/baro-admin-check rtk proxy pnpm seed:admin admin@baro.dev password1234` 후 같은 PGLITE_DATA_DIR로 api dev + web dev 기동(백그라운드), curl로:

```bash
# 로그인 → 쿠키 저장
curl -s -c /tmp/adm.txt -X POST http://localhost:8000/api/auth/sign-in/email \
  -H 'content-type: application/json' \
  -d '{"email":"admin@baro.dev","password":"password1234"}'
# stats
curl -s -b /tmp/adm.txt http://localhost:8000/api/admin/stats
# admin listUsers (better-auth admin 엔드포인트)
curl -s -b /tmp/adm.txt "http://localhost:8000/api/auth/admin/list-users?limit=10"
```

Expected: stats에 `totalUsers>=1`, list-users에 admin@baro.dev 포함. 프로세스 종료, /tmp 데이터 삭제.

- [ ] **Step 7: Commit**

```bash
git add apps/web pnpm-lock.yaml
git commit -m "feat(web): add admin dashboard with user management and signup chart"
```

---

### Task 4: 게이트 + CI

**Files:** (필요 시 lint 수정만)

- [ ] **Step 1:** `rtk proxy pnpm check` 그린 확인 (실패 시 수정 후 재실행, 보고)
- [ ] **Step 2:** `rtk proxy pnpm install --frozen-lockfile && git status --short` — 성공 + 클린
- [ ] **Step 3:** push 후 CI 확인 — gh 인증됨:

```bash
git push -u origin feat/admin
```

브랜치 push는 CI 트리거 안 됨(main push/PR만). 보고만 하고 종료 — 컨트롤러가 머지 후 `gh run watch`로 확인.

---

## 완료 기준 (Plan 3)

- `pnpm seed:admin` 1회 → `/admin` 접근 가능
- 유저 목록/검색/정지/해제/역할 변경 동작 (api가 401/403 집행)
- 총 가입자 + 14일 추이 차트 표시
- 스키마 마이그레이션 diff 0 (Plan 2 선반영 검증)
- `pnpm check` + main CI 그린
- 다음: Plan 4 예제 기능(todos) + Playwright E2E smoke 3개(가입→로그인→관리자)

## 리스크 노트

- Better Auth admin 플러그인의 listUsers 쿼리 파라미터·응답 형태는 1.6.23 문서 기준 — 편차 시 실제 API에 맞추고 보고
- role 승격 후 기존 세션의 role 스냅샷 문제 — 재로그인으로 해결, 테스트도 그 순서로
- Base UI 컴포넌트 API는 설치 버전 기준 조정 허용 (동작·접근성이 요구사항)
- PGlite 테스트 타임아웃 30s 설정 존재 (CI 러너 느림) — 새 vitest 설정 만들지 말고 기존 파일 유지
