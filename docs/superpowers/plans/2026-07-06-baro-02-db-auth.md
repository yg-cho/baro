# baro Plan 2/6 — DB(Drizzle + PGlite/Postgres) + 인증(Better Auth) 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** DATABASE_URL 없이는 PGlite 임베디드로, 있으면 Postgres로 붙는 Drizzle DB 레이어 위에 Better Auth 인증(이메일/비번 + 조건부 소셜)을 올리고, web에 로그인/가입/재설정 페이지를 완성한다.

**Architecture:** `packages/db`가 스키마+듀얼 드라이버 클라이언트+마이그레이션을 소유하고, `packages/auth`가 `createAuth(db)` 팩토리로 Better Auth 인스턴스를 만든다. api는 `/api/auth/*`에 핸들러를 마운트하고 부팅 시 자동 마이그레이션. web은 `better-auth/react` 클라이언트로 세션 관리. 소셜 로그인은 env 키 존재 시에만 활성 — api의 `/api/auth/providers` 라우트(RPC 타입 공유)로 web이 버튼 표시 여부 결정.

**Tech Stack:** drizzle-orm, @electric-sql/pglite, pg(node-postgres), drizzle-kit(마이그레이션 SQL 생성), better-auth ^1.3, better-auth/react

**참조 스펙:** `docs/superpowers/specs/2026-07-05-baro-fullstack-design.md` §4.1
**선행:** Plan 1 완료 상태 (main, CI 그린)

## Global Constraints

- 패키지 네이밍: `@baro/<이름>` 스코프, 전부 `"private": true`
- 포트: web `3000`, api `8000`
- 코드젠 금지 — 타입은 TS 추론으로만 공유. 예외: drizzle-kit의 마이그레이션 SQL 생성(개발 시 1회, 산출물 커밋)은 허용 — 빌드 파이프라인에 코드젠 없음
- 내부 패키지는 TS 소스 직접 export (빌드 스텝 없음)
- 린트/포맷 Biome 하나만. 커밋은 Conventional Commits
- `apps/api/src/app.ts`의 체인 순서 주석 준수 — `.doc()` 먼저, `.route()` 마지막 유지
- 버전이 해석 안 되면 가장 가까운 호환 버전 설치 후 보고서에 기록
- 로컬 환경 주의: rtk 훅 경유 pnpm이 exit 254 나면 `rtk proxy pnpm <동일 인자>`로 재시도
- **lockfile 규칙 (Plan 1 교훈): package.json의 의존성 범위를 바꿨으면 반드시 `pnpm install` 후 `git status --short` 출력을 보고서에 붙이고, 변경된 pnpm-lock.yaml을 같은 커밋에 포함할 것.** frozen-lockfile CI가 specifier 불일치로 죽는다

---

### Task 1: packages/db — 스키마 + 듀얼 드라이버 + 마이그레이션

**Files:**
- Create: `packages/db/package.json`
- Create: `packages/db/tsconfig.json`
- Create: `packages/db/drizzle.config.ts`
- Create: `packages/db/src/schema.ts`
- Create: `packages/db/src/client.ts`
- Create: `packages/db/src/migrate.ts`
- Create: `packages/db/src/index.ts`
- Create: `packages/db/drizzle/` (drizzle-kit generate 산출 SQL — 커밋 대상)
- Test: `packages/db/src/db.test.ts`

**Interfaces:**
- Consumes: `tsconfig.base.json`
- Produces:
  - `@baro/db`에서 `createDb(options?: { url?: string; dataDir?: string }): Db` — DATABASE_URL 있으면 pg, 없으면 PGlite(`dataDir` 기본 `.baro/pglite`, `"memory://"` 지원)
  - `db` 지연 싱글톤 (env 기반), `type Db`
  - `runMigrations(db: Db): Promise<void>` — 커밋된 SQL 마이그레이션 적용
  - `schema` — `user`, `session`, `account`, `verification` 테이블 (Better Auth 코어 + admin 플러그인 필드 선반영)

- [ ] **Step 1: package.json 작성**

```json
{
  "name": "@baro/db",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./schema": "./src/schema.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "db:generate": "drizzle-kit generate"
  },
  "dependencies": {
    "@electric-sql/pglite": "^0.3.0",
    "drizzle-orm": "^0.44.0",
    "pg": "^8.16.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/pg": "^8.15.0",
    "drizzle-kit": "^0.31.0",
    "typescript": "^5.8.0",
    "vitest": "^3.0.0"
  }
}
```

- [ ] **Step 2: tsconfig.json 작성**

```json
{
  "extends": "../../tsconfig.base.json",
  "include": ["src", "drizzle.config.ts"]
}
```

- [ ] **Step 3: 스키마 작성**

`packages/db/src/schema.ts` — Better Auth 코어 4테이블. admin 플러그인 필드(role/banned/banReason/banExpires, session.impersonatedBy)는 Plan 3에서 쓰지만 지금 넣어 마이그레이션 1회로 끝냄:

```ts
import { boolean, pgTable, text, timestamp } from "drizzle-orm/pg-core";

export const user = pgTable("user", {
  id: text("id").primaryKey(),
  name: text("name").notNull(),
  email: text("email").notNull().unique(),
  emailVerified: boolean("email_verified").notNull().default(false),
  image: text("image"),
  // admin plugin fields (Plan 3) — included now to avoid a second migration
  role: text("role").notNull().default("user"),
  banned: boolean("banned").notNull().default(false),
  banReason: text("ban_reason"),
  banExpires: timestamp("ban_expires"),
  createdAt: timestamp("created_at").notNull().defaultNow(),
  updatedAt: timestamp("updated_at").notNull().defaultNow(),
});

export const session = pgTable("session", {
  id: text("id").primaryKey(),
  expiresAt: timestamp("expires_at").notNull(),
  token: text("token").notNull().unique(),
  ipAddress: text("ip_address"),
  userAgent: text("user_agent"),
  userId: text("user_id")
    .notNull()
    .references(() => user.id, { onDelete: "cascade" }),
  impersonatedBy: text("impersonated_by"),
  createdAt: timestamp("created_at").notNull().defaultNow(),
  updatedAt: timestamp("updated_at").notNull().defaultNow(),
});

export const account = pgTable("account", {
  id: text("id").primaryKey(),
  accountId: text("account_id").notNull(),
  providerId: text("provider_id").notNull(),
  userId: text("user_id")
    .notNull()
    .references(() => user.id, { onDelete: "cascade" }),
  accessToken: text("access_token"),
  refreshToken: text("refresh_token"),
  idToken: text("id_token"),
  accessTokenExpiresAt: timestamp("access_token_expires_at"),
  refreshTokenExpiresAt: timestamp("refresh_token_expires_at"),
  scope: text("scope"),
  password: text("password"),
  createdAt: timestamp("created_at").notNull().defaultNow(),
  updatedAt: timestamp("updated_at").notNull().defaultNow(),
});

export const verification = pgTable("verification", {
  id: text("id").primaryKey(),
  identifier: text("identifier").notNull(),
  value: text("value").notNull(),
  expiresAt: timestamp("expires_at").notNull(),
  createdAt: timestamp("created_at").notNull().defaultNow(),
  updatedAt: timestamp("updated_at").notNull().defaultNow(),
});
```

주의: Better Auth 1.x의 drizzle 어댑터가 기대하는 필드명과 일치해야 함. 구현 중 `better-auth` 문서의 코어 스키마와 대조하고, 어댑터가 camelCase 필드를 찾지 못하면 어댑터 옵션(`usePlural: false` 등) 또는 필드 매핑을 조정하고 보고서에 기록할 것.

- [ ] **Step 4: drizzle.config.ts 작성**

```ts
import { defineConfig } from "drizzle-kit";

export default defineConfig({
  dialect: "postgresql",
  schema: "./src/schema.ts",
  out: "./drizzle",
});
```

- [ ] **Step 5: 실패하는 테스트 작성**

`packages/db/src/db.test.ts`:

```ts
import { describe, expect, it } from "vitest";
import { createDb, runMigrations } from "./index";
import { user } from "./schema";

describe("db", () => {
  it("migrates and inserts a user on in-memory pglite", async () => {
    const db = createDb({ dataDir: "memory://" });
    await runMigrations(db);

    await db.insert(user).values({
      id: "u1",
      name: "Test",
      email: "t@example.com",
    });

    const rows = await db.select().from(user);
    expect(rows).toHaveLength(1);
    expect(rows[0].email).toBe("t@example.com");
    expect(rows[0].role).toBe("user");
  });
});
```

- [ ] **Step 6: 테스트 실패 확인**

Run: `pnpm install && pnpm --filter @baro/db test`
Expected: FAIL — `./index` 모듈 없음

- [ ] **Step 7: 클라이언트 + 마이그레이터 구현**

`packages/db/src/client.ts`:

```ts
import { PGlite } from "@electric-sql/pglite";
import { drizzle as drizzleNodePg } from "drizzle-orm/node-postgres";
import { drizzle as drizzlePglite } from "drizzle-orm/pglite";
import { Pool } from "pg";
import * as schema from "./schema";

export type CreateDbOptions = {
  /** Postgres connection string. Falls back to DATABASE_URL. */
  url?: string;
  /** PGlite data directory. "memory://" for in-memory. Default ".baro/pglite". */
  dataDir?: string;
};

export function createDb(options: CreateDbOptions = {}) {
  const url = options.url ?? process.env.DATABASE_URL;
  if (url) {
    const pool = new Pool({ connectionString: url });
    return drizzleNodePg(pool, { schema });
  }
  const client = new PGlite(
    options.dataDir ?? process.env.PGLITE_DATA_DIR ?? ".baro/pglite",
  );
  return drizzlePglite(client, { schema });
}

export type Db = ReturnType<typeof createDb>;
```

`packages/db/src/migrate.ts` (드라이버별 마이그레이터 분기 — 커밋된 `drizzle/` SQL 적용):

```ts
import { dirname, join } from "node:path";
import { fileURLToPath } from "node:url";
import { migrate as migrateNodePg } from "drizzle-orm/node-postgres/migrator";
import { migrate as migratePglite } from "drizzle-orm/pglite/migrator";
import type { Db } from "./client";

const migrationsFolder = join(
  dirname(fileURLToPath(import.meta.url)),
  "../drizzle",
);

export async function runMigrations(db: Db): Promise<void> {
  // drizzle instance driver detection: pglite instances expose $client of PGlite
  const client = (db as { $client?: unknown }).$client;
  const isPglite =
    client !== undefined &&
    client !== null &&
    client.constructor.name === "PGlite";

  if (isPglite) {
    // eslint 없음 — 타입은 각 마이그레이터 시그니처에 맞춰 단언
    await migratePglite(db as Parameters<typeof migratePglite>[0], {
      migrationsFolder,
    });
  } else {
    await migrateNodePg(db as Parameters<typeof migrateNodePg>[0], {
      migrationsFolder,
    });
  }
}
```

`packages/db/src/index.ts`:

```ts
export { createDb, type CreateDbOptions, type Db } from "./client";
export { runMigrations } from "./migrate";
export * as schema from "./schema";

import { createDb } from "./client";
import type { Db } from "./client";

let _db: Db | undefined;
/** Lazy singleton — env 기반. 테스트는 createDb()로 직접 생성. */
export function getDb(): Db {
  _db ??= createDb();
  return _db;
}
```

- [ ] **Step 8: 마이그레이션 SQL 생성 (1회, 산출물 커밋)**

Run: `pnpm --filter @baro/db db:generate`
Expected: `packages/db/drizzle/0000_*.sql` + `drizzle/meta/` 생성. 내용에 4개 테이블 CREATE TABLE 포함 확인.

- [ ] **Step 9: 테스트 통과 확인**

Run: `pnpm --filter @baro/db test && pnpm --filter @baro/db typecheck`
Expected: PASS — 1 passed, typecheck 에러 없음

- [ ] **Step 10: .gitignore에 PGlite 데이터 디렉토리 추가**

루트 `.gitignore`에 추가:

```
.baro/
```

- [ ] **Step 11: Commit**

```bash
git add packages/db .gitignore pnpm-lock.yaml
git commit -m "feat(db): add drizzle schema with dual pglite/postgres driver and migrations"
```

---

### Task 2: packages/auth — Better Auth 팩토리

**Files:**
- Create: `packages/auth/package.json`
- Create: `packages/auth/tsconfig.json`
- Create: `packages/auth/src/index.ts`
- Test: `packages/auth/src/auth.test.ts`

**Interfaces:**
- Consumes: `createDb`, `runMigrations`, `type Db` (`@baro/db`, Task 1)
- Produces:
  - `createAuth(db: Db): Auth` — Better Auth 인스턴스 팩토리. `basePath: "/api/auth"`
  - `enabledSocialProviders(): { google: boolean; github: boolean }` — env 키 존재 여부
  - `type Auth = ReturnType<typeof createAuth>`
  - Task 3의 api가 `auth.handler`를 마운트, Task 4의 web이 같은 엔드포인트로 통신

- [ ] **Step 1: package.json 작성**

```json
{
  "name": "@baro/auth",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@baro/db": "workspace:*",
    "better-auth": "^1.3.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.8.0",
    "vitest": "^3.0.0"
  }
}
```

- [ ] **Step 2: tsconfig.json 작성**

```json
{
  "extends": "../../tsconfig.base.json",
  "include": ["src"]
}
```

- [ ] **Step 3: 실패하는 테스트 작성**

`packages/auth/src/auth.test.ts`:

```ts
import { createDb, runMigrations } from "@baro/db";
import { beforeAll, describe, expect, it } from "vitest";
import { createAuth } from "./index";

describe("auth", () => {
  let auth: ReturnType<typeof createAuth>;

  beforeAll(async () => {
    const db = createDb({ dataDir: "memory://" });
    await runMigrations(db);
    auth = createAuth(db);
  });

  it("signs up a user with email/password", async () => {
    const res = await auth.api.signUpEmail({
      body: {
        name: "Test User",
        email: "signup@example.com",
        password: "password1234",
      },
    });
    expect(res.user.email).toBe("signup@example.com");
  });

  it("signs in and returns a session", async () => {
    await auth.api.signUpEmail({
      body: {
        name: "Test User 2",
        email: "signin@example.com",
        password: "password1234",
      },
    });
    const res = await auth.api.signInEmail({
      body: { email: "signin@example.com", password: "password1234" },
      asResponse: true,
    });
    expect(res.status).toBe(200);
    expect(res.headers.get("set-cookie")).toContain("better-auth");
  });
});
```

- [ ] **Step 4: 테스트 실패 확인**

Run: `pnpm install && pnpm --filter @baro/auth test`
Expected: FAIL — `createAuth` 없음

- [ ] **Step 5: 구현**

`packages/auth/src/index.ts`:

```ts
import type { Db } from "@baro/db";
import { schema } from "@baro/db";
import { betterAuth } from "better-auth";
import { drizzleAdapter } from "better-auth/adapters/drizzle";

export function enabledSocialProviders() {
  return {
    google: Boolean(
      process.env.GOOGLE_CLIENT_ID && process.env.GOOGLE_CLIENT_SECRET,
    ),
    github: Boolean(
      process.env.GITHUB_CLIENT_ID && process.env.GITHUB_CLIENT_SECRET,
    ),
  };
}

export function createAuth(db: Db) {
  const social = enabledSocialProviders();

  return betterAuth({
    basePath: "/api/auth",
    secret: process.env.BETTER_AUTH_SECRET ?? "baro-dev-secret-change-me",
    baseURL: process.env.BETTER_AUTH_URL ?? "http://localhost:8000",
    trustedOrigins: [process.env.WEB_ORIGIN ?? "http://localhost:3000"],
    database: drizzleAdapter(db, { provider: "pg", schema }),
    emailAndPassword: {
      enabled: true,
      sendResetPassword: async ({ user, url }) => {
        // dev: console output. production: replace with SMTP (docs/recipes)
        console.log(`[baro auth] password reset for ${user.email}: ${url}`);
      },
    },
    emailVerification: {
      sendVerificationEmail: async ({ user, url }) => {
        console.log(`[baro auth] verify email for ${user.email}: ${url}`);
      },
    },
    socialProviders: {
      ...(social.google && {
        google: {
          clientId: process.env.GOOGLE_CLIENT_ID as string,
          clientSecret: process.env.GOOGLE_CLIENT_SECRET as string,
        },
      }),
      ...(social.github && {
        github: {
          clientId: process.env.GITHUB_CLIENT_ID as string,
          clientSecret: process.env.GITHUB_CLIENT_SECRET as string,
        },
      }),
    },
  });
}

export type Auth = ReturnType<typeof createAuth>;
```

주의: better-auth 1.x에서 API 명칭이 다르면(`signUpEmail` 등) 설치된 버전의 실제 시그니처에 맞추고 테스트도 같이 조정, 보고서에 기록. drizzle 어댑터가 스키마 테이블을 못 찾으면 `drizzleAdapter(db, { provider: "pg", schema: { user: schema.user, session: schema.session, account: schema.account, verification: schema.verification } })` 형태로 명시.

- [ ] **Step 6: 테스트 통과 확인**

Run: `pnpm --filter @baro/auth test && pnpm --filter @baro/auth typecheck`
Expected: PASS — 2 passed, typecheck 클린

- [ ] **Step 7: Commit**

```bash
git add packages/auth pnpm-lock.yaml
git commit -m "feat(auth): add better-auth factory with conditional social providers"
```

---

### Task 3: apps/api — 인증 마운트 + 부팅 마이그레이션

**Files:**
- Create: `apps/api/src/modules/auth/routes.ts`
- Modify: `apps/api/src/app.ts`
- Modify: `apps/api/src/index.ts`
- Modify: `apps/api/package.json` (deps 추가)
- Create: `apps/api/.env.example`
- Create: `packages/shared/src/schemas/auth.ts`
- Test: `apps/api/src/modules/auth/routes.test.ts`

**Interfaces:**
- Consumes: `createAuth`, `enabledSocialProviders` (Task 2), `getDb`, `runMigrations` (Task 1)
- Produces:
  - `GET /api/auth/providers` → `{ email: true, google: boolean, github: boolean }` (RPC 타입 전파 — web이 소셜 버튼 표시 판단)
  - `/api/auth/*` Better Auth 핸들러 (sign-up/sign-in/sign-out/session 등)
  - CORS `credentials: true` — web의 쿠키 인증 필수
  - api 부팅 시 `runMigrations` 자동 실행

- [ ] **Step 1: shared 스키마 추가**

`packages/shared/src/schemas/auth.ts`:

```ts
import { z } from "zod";

export const authProvidersSchema = z.object({
  email: z.literal(true),
  google: z.boolean(),
  github: z.boolean(),
});

export type AuthProviders = z.infer<typeof authProvidersSchema>;
```

- [ ] **Step 2: api deps 추가**

`apps/api/package.json` dependencies에 추가:

```json
"@baro/auth": "workspace:*",
"@baro/db": "workspace:*"
```

- [ ] **Step 3: 실패하는 테스트 작성**

`apps/api/src/modules/auth/routes.test.ts`:

```ts
import { describe, expect, it } from "vitest";
import { app } from "../../app";

describe("auth module", () => {
  it("GET /api/auth/providers returns provider flags", async () => {
    const res = await app.request("/api/auth/providers");
    expect(res.status).toBe(200);
    const body = await res.json();
    expect(body.email).toBe(true);
    expect(typeof body.google).toBe("boolean");
  });

  it("signs up via HTTP and gets a session cookie", async () => {
    const res = await app.request("/api/auth/sign-up/email", {
      method: "POST",
      headers: { "content-type": "application/json" },
      body: JSON.stringify({
        name: "HTTP User",
        email: "http@example.com",
        password: "password1234",
      }),
    });
    expect(res.status).toBe(200);
    expect(res.headers.get("set-cookie")).toContain("better-auth");
  });

  it("returns the session for a signed-in user", async () => {
    const signUp = await app.request("/api/auth/sign-up/email", {
      method: "POST",
      headers: { "content-type": "application/json" },
      body: JSON.stringify({
        name: "Session User",
        email: "session@example.com",
        password: "password1234",
      }),
    });
    const cookie = signUp.headers.get("set-cookie") ?? "";
    const res = await app.request("/api/auth/get-session", {
      headers: { cookie },
    });
    expect(res.status).toBe(200);
    const body = await res.json();
    expect(body.user.email).toBe("session@example.com");
  });
});
```

테스트가 in-memory DB를 쓰도록: 테스트 환경에서 `PGLITE_DATA_DIR=memory://`가 필요. `apps/api/vitest.config.ts` 생성:

```ts
import { defineConfig } from "vitest/config";

export default defineConfig({
  test: {
    env: {
      PGLITE_DATA_DIR: "memory://",
    },
    setupFiles: ["./src/test-setup.ts"],
  },
});
```

`apps/api/src/test-setup.ts` (마이그레이션 1회 실행):

```ts
import { getDb, runMigrations } from "@baro/db";

await runMigrations(getDb());
```

- [ ] **Step 4: 테스트 실패 확인**

Run: `pnpm install && pnpm --filter @baro/api test`
Expected: FAIL — `/api/auth/providers` 404 (라우트 없음)

- [ ] **Step 5: auth 모듈 구현**

`apps/api/src/modules/auth/routes.ts`:

```ts
import { createAuth, enabledSocialProviders } from "@baro/auth";
import { getDb } from "@baro/db";
import { authProvidersSchema } from "@baro/shared/schemas/auth";
import { createRoute, OpenAPIHono } from "@hono/zod-openapi";

export const auth = createAuth(getDb());

export const authRoutes = new OpenAPIHono()
  .openapi(
    createRoute({
      method: "get",
      path: "/providers",
      responses: {
        200: {
          description: "Enabled auth providers",
          content: {
            "application/json": { schema: authProvidersSchema },
          },
        },
      },
    }),
    (c) => {
      const social = enabledSocialProviders();
      return c.json({ email: true as const, ...social }, 200);
    },
  )
  .on(["GET", "POST"], "/*", (c) => auth.handler(c.req.raw));
```

주의: `/providers`를 와일드카드 핸들러보다 먼저 등록 (등록 순서 = 매칭 순서).

- [ ] **Step 6: app.ts 수정**

`apps/api/src/app.ts` — cors에 `credentials: true` 추가, auth 라우트 마운트. 기존 체인 순서 주석 유지, `.route()`는 여전히 마지막 그룹:

```ts
import { OpenAPIHono } from "@hono/zod-openapi";
import { cors } from "hono/cors";
import { authRoutes } from "./modules/auth/routes";
import { healthRoutes } from "./modules/health/routes";

// Order matters: .doc() must come before .use()/.route() and .route() must stay
// chained last so AppType keeps full route typing for the web RPC client.
// Side effect: /openapi.json intentionally gets no CORS. Do not "fix" the order.
export const app = new OpenAPIHono()
  .use(
    cors({
      origin: process.env.WEB_ORIGIN ?? "http://localhost:3000",
      credentials: true,
    }),
  )
  .route("/health", healthRoutes)
  .route("/api/auth", authRoutes);

app.doc("/openapi.json", {
  openapi: "3.1.0",
  info: { title: "baro API", version: "0.0.0" },
});

export type AppType = typeof app;
```

(주석 위치·`.doc()` 순서가 현재 코드와 다르면 현재 코드의 검증된 순서를 유지한 채 authRoutes만 추가할 것 — 체인 마지막 `.route()` 뒤에 이어 붙이기.)

- [ ] **Step 7: 부팅 마이그레이션**

`apps/api/src/index.ts`:

```ts
import { serve } from "@hono/node-server";
import { getDb, runMigrations } from "@baro/db";
import { app } from "./app";

const port = Number(process.env.PORT ?? 8000);

await runMigrations(getDb());

serve({ fetch: app.fetch, port }, (info) => {
  console.log(`baro api listening on http://localhost:${info.port}`);
});
```

- [ ] **Step 8: .env.example 작성**

`apps/api/.env.example`:

```
# Server
PORT=8000
WEB_ORIGIN=http://localhost:3000

# Database — leave DATABASE_URL unset to use embedded PGlite (.baro/pglite)
# DATABASE_URL=postgres://user:pass@localhost:5432/baro
# PGLITE_DATA_DIR=.baro/pglite

# Auth — generate a real secret: openssl rand -base64 32
BETTER_AUTH_SECRET=baro-dev-secret-change-me
BETTER_AUTH_URL=http://localhost:8000

# Social login (optional — buttons appear automatically when keys are set)
# GOOGLE_CLIENT_ID=
# GOOGLE_CLIENT_SECRET=
# GITHUB_CLIENT_ID=
# GITHUB_CLIENT_SECRET=
```

- [ ] **Step 9: 테스트 통과 확인**

Run: `pnpm --filter @baro/api test && pnpm --filter @baro/api typecheck`
Expected: PASS — 4 passed (health 1 + auth 3), typecheck 클린

- [ ] **Step 10: 수동 확인**

Run: api dev 백그라운드 기동 후

```bash
curl -s http://localhost:8000/api/auth/providers
```

Expected: `{"email":true,"google":false,"github":false}` (키 미설정 기준). 확인 후 종료. `.baro/pglite` 디렉토리 생성 확인 (임베디드 영속화 동작 증거).

- [ ] **Step 11: Commit**

```bash
git add apps/api packages/shared pnpm-lock.yaml
git commit -m "feat(api): mount better-auth with providers route and boot-time migrations"
```

---

### Task 4: apps/web — 인증 페이지 + 세션

**Files:**
- Create: `apps/web/src/lib/auth-client.ts`
- Create: `apps/web/src/components/ui/button.tsx`
- Create: `apps/web/src/components/ui/input.tsx`
- Create: `apps/web/src/components/ui/label.tsx`
- Create: `apps/web/src/components/ui/card.tsx`
- Create: `apps/web/src/features/auth/auth-form.tsx`
- Create: `apps/web/src/app/(auth)/login/page.tsx`
- Create: `apps/web/src/app/(auth)/signup/page.tsx`
- Create: `apps/web/src/app/(auth)/forgot-password/page.tsx`
- Create: `apps/web/src/app/(auth)/reset-password/page.tsx`
- Create: `apps/web/src/features/auth/session-widget.tsx`
- Modify: `apps/web/src/app/page.tsx`
- Modify: `apps/web/package.json` (better-auth 추가)

**Interfaces:**
- Consumes: `/api/auth/*` 엔드포인트, `GET /api/auth/providers` RPC (Task 3)
- Produces:
  - `authClient` (`better-auth/react`) — `signIn`, `signUp`, `signOut`, `useSession`
  - 페이지: `/login`, `/signup`, `/forgot-password`, `/reset-password`
  - `SessionWidget` — 홈에서 로그인 상태 표시 + 로그아웃
  - ui 컴포넌트 4개 (shadcn 스타일 — 코드 소유, Tailwind만. Base UI 프리미티브는 다이얼로그/메뉴가 필요해지는 Plan 3에서 도입)

- [ ] **Step 1: better-auth 의존성 추가**

`apps/web/package.json` dependencies에 `"better-auth": "^1.3.0"` 추가 (packages/auth와 동일 버전 — 불일치 시 타입 충돌).

- [ ] **Step 2: auth 클라이언트 작성**

`apps/web/src/lib/auth-client.ts`:

```ts
import { createAuthClient } from "better-auth/react";

export const authClient = createAuthClient({
  baseURL: `${process.env.NEXT_PUBLIC_API_URL ?? "http://localhost:8000"}/api/auth`,
});

export const { signIn, signUp, signOut, useSession } = authClient;
```

- [ ] **Step 3: ui 컴포넌트 4개 작성 (shadcn 스타일, 코드 소유)**

`apps/web/src/components/ui/button.tsx`:

```tsx
import { type ButtonHTMLAttributes, forwardRef } from "react";

type Variant = "default" | "outline" | "ghost";

const variants: Record<Variant, string> = {
  default:
    "bg-zinc-900 text-white hover:bg-zinc-700 dark:bg-zinc-100 dark:text-zinc-900 dark:hover:bg-zinc-300",
  outline:
    "border border-zinc-300 hover:bg-zinc-100 dark:border-zinc-700 dark:hover:bg-zinc-800",
  ghost: "hover:bg-zinc-100 dark:hover:bg-zinc-800",
};

export const Button = forwardRef<
  HTMLButtonElement,
  ButtonHTMLAttributes<HTMLButtonElement> & { variant?: Variant }
>(({ variant = "default", className = "", ...props }, ref) => (
  <button
    ref={ref}
    className={`inline-flex h-9 items-center justify-center rounded-md px-4 text-sm font-medium transition-colors disabled:pointer-events-none disabled:opacity-50 ${variants[variant]} ${className}`}
    {...props}
  />
));
Button.displayName = "Button";
```

`apps/web/src/components/ui/input.tsx`:

```tsx
import { forwardRef, type InputHTMLAttributes } from "react";

export const Input = forwardRef<
  HTMLInputElement,
  InputHTMLAttributes<HTMLInputElement>
>(({ className = "", ...props }, ref) => (
  <input
    ref={ref}
    className={`h-9 w-full rounded-md border border-zinc-300 bg-transparent px-3 text-sm outline-none focus:ring-2 focus:ring-zinc-400 dark:border-zinc-700 ${className}`}
    {...props}
  />
));
Input.displayName = "Input";
```

`apps/web/src/components/ui/label.tsx`:

```tsx
import type { LabelHTMLAttributes } from "react";

export function Label({
  className = "",
  ...props
}: LabelHTMLAttributes<HTMLLabelElement>) {
  return (
    // biome-ignore lint/a11y/noLabelWithoutControl: htmlFor passed by callers
    <label className={`text-sm font-medium ${className}`} {...props} />
  );
}
```

`apps/web/src/components/ui/card.tsx`:

```tsx
import type { HTMLAttributes } from "react";

export function Card({
  className = "",
  ...props
}: HTMLAttributes<HTMLDivElement>) {
  return (
    <div
      className={`rounded-xl border border-zinc-200 bg-white p-6 shadow-sm dark:border-zinc-800 dark:bg-zinc-950 ${className}`}
      {...props}
    />
  );
}
```

- [ ] **Step 4: 공용 AuthForm 작성**

`apps/web/src/features/auth/auth-form.tsx` — 로그인/가입 공용, 소셜 버튼은 providers RPC로 조건 표시:

```tsx
"use client";

import { api } from "@/lib/api";
import { signIn, signUp } from "@/lib/auth-client";
import { Button } from "@/components/ui/button";
import { Card } from "@/components/ui/card";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import Link from "next/link";
import { useRouter } from "next/navigation";
import { useEffect, useState } from "react";

type Mode = "login" | "signup";

export function AuthForm({ mode }: { mode: Mode }) {
  const router = useRouter();
  const [error, setError] = useState<string | null>(null);
  const [pending, setPending] = useState(false);
  const [social, setSocial] = useState({ google: false, github: false });

  useEffect(() => {
    api.api.auth.providers
      .$get()
      .then((res) => res.json())
      .then((p) => setSocial({ google: p.google, github: p.github }))
      .catch(() => {});
  }, []);

  async function onSubmit(e: React.FormEvent<HTMLFormElement>) {
    e.preventDefault();
    setPending(true);
    setError(null);
    const form = new FormData(e.currentTarget);
    const email = String(form.get("email"));
    const password = String(form.get("password"));

    const result =
      mode === "signup"
        ? await signUp.email({
            name: String(form.get("name")),
            email,
            password,
          })
        : await signIn.email({ email, password });

    setPending(false);
    if (result.error) {
      setError(result.error.message ?? "Something went wrong");
      return;
    }
    router.push("/");
    router.refresh();
  }

  return (
    <Card className="w-full max-w-sm">
      <h1 className="mb-4 text-lg font-semibold">
        {mode === "login" ? "Sign in to baro" : "Create your account"}
      </h1>
      <form onSubmit={onSubmit} className="flex flex-col gap-3">
        {mode === "signup" && (
          <div className="flex flex-col gap-1">
            <Label htmlFor="name">Name</Label>
            <Input id="name" name="name" required />
          </div>
        )}
        <div className="flex flex-col gap-1">
          <Label htmlFor="email">Email</Label>
          <Input id="email" name="email" type="email" required />
        </div>
        <div className="flex flex-col gap-1">
          <Label htmlFor="password">Password</Label>
          <Input
            id="password"
            name="password"
            type="password"
            minLength={8}
            required
          />
        </div>
        {error && <p className="text-sm text-red-600">{error}</p>}
        <Button type="submit" disabled={pending}>
          {mode === "login" ? "Sign in" : "Sign up"}
        </Button>
      </form>

      {(social.google || social.github) && (
        <div className="mt-4 flex flex-col gap-2">
          {social.google && (
            <Button
              variant="outline"
              onClick={() => signIn.social({ provider: "google" })}
            >
              Continue with Google
            </Button>
          )}
          {social.github && (
            <Button
              variant="outline"
              onClick={() => signIn.social({ provider: "github" })}
            >
              Continue with GitHub
            </Button>
          )}
        </div>
      )}

      <p className="mt-4 text-sm text-zinc-500">
        {mode === "login" ? (
          <>
            No account?{" "}
            <Link className="underline" href="/signup">
              Sign up
            </Link>
            {" · "}
            <Link className="underline" href="/forgot-password">
              Forgot password
            </Link>
          </>
        ) : (
          <>
            Already have an account?{" "}
            <Link className="underline" href="/login">
              Sign in
            </Link>
          </>
        )}
      </p>
    </Card>
  );
}
```

- [ ] **Step 5: 페이지 4개 작성**

`apps/web/src/app/(auth)/login/page.tsx`:

```tsx
import { AuthForm } from "@/features/auth/auth-form";

export default function LoginPage() {
  return (
    <main className="flex min-h-screen items-center justify-center p-4">
      <AuthForm mode="login" />
    </main>
  );
}
```

`apps/web/src/app/(auth)/signup/page.tsx`:

```tsx
import { AuthForm } from "@/features/auth/auth-form";

export default function SignupPage() {
  return (
    <main className="flex min-h-screen items-center justify-center p-4">
      <AuthForm mode="signup" />
    </main>
  );
}
```

`apps/web/src/app/(auth)/forgot-password/page.tsx`:

```tsx
"use client";

import { authClient } from "@/lib/auth-client";
import { Button } from "@/components/ui/button";
import { Card } from "@/components/ui/card";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { useState } from "react";

export default function ForgotPasswordPage() {
  const [sent, setSent] = useState(false);

  async function onSubmit(e: React.FormEvent<HTMLFormElement>) {
    e.preventDefault();
    const email = String(new FormData(e.currentTarget).get("email"));
    await authClient.requestPasswordReset({
      email,
      redirectTo: "/reset-password",
    });
    setSent(true); // always confirm — do not leak which emails exist
  }

  return (
    <main className="flex min-h-screen items-center justify-center p-4">
      <Card className="w-full max-w-sm">
        <h1 className="mb-4 text-lg font-semibold">Reset your password</h1>
        {sent ? (
          <p className="text-sm">
            If that account exists, a reset link has been sent. In dev mode,
            check the api console.
          </p>
        ) : (
          <form onSubmit={onSubmit} className="flex flex-col gap-3">
            <div className="flex flex-col gap-1">
              <Label htmlFor="email">Email</Label>
              <Input id="email" name="email" type="email" required />
            </div>
            <Button type="submit">Send reset link</Button>
          </form>
        )}
      </Card>
    </main>
  );
}
```

`apps/web/src/app/(auth)/reset-password/page.tsx`:

```tsx
"use client";

import { authClient } from "@/lib/auth-client";
import { Button } from "@/components/ui/button";
import { Card } from "@/components/ui/card";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { useRouter, useSearchParams } from "next/navigation";
import { Suspense, useState } from "react";

function ResetForm() {
  const router = useRouter();
  const token = useSearchParams().get("token") ?? "";
  const [error, setError] = useState<string | null>(null);

  async function onSubmit(e: React.FormEvent<HTMLFormElement>) {
    e.preventDefault();
    const newPassword = String(
      new FormData(e.currentTarget).get("password"),
    );
    const result = await authClient.resetPassword({ newPassword, token });
    if (result.error) {
      setError(result.error.message ?? "Reset failed");
      return;
    }
    router.push("/login");
  }

  return (
    <Card className="w-full max-w-sm">
      <h1 className="mb-4 text-lg font-semibold">Choose a new password</h1>
      <form onSubmit={onSubmit} className="flex flex-col gap-3">
        <div className="flex flex-col gap-1">
          <Label htmlFor="password">New password</Label>
          <Input
            id="password"
            name="password"
            type="password"
            minLength={8}
            required
          />
        </div>
        {error && <p className="text-sm text-red-600">{error}</p>}
        <Button type="submit">Reset password</Button>
      </form>
    </Card>
  );
}

export default function ResetPasswordPage() {
  return (
    <main className="flex min-h-screen items-center justify-center p-4">
      <Suspense>
        <ResetForm />
      </Suspense>
    </main>
  );
}
```

- [ ] **Step 6: 세션 위젯 + 홈 반영**

`apps/web/src/features/auth/session-widget.tsx`:

```tsx
"use client";

import { signOut, useSession } from "@/lib/auth-client";
import { Button } from "@/components/ui/button";
import Link from "next/link";
import { useRouter } from "next/navigation";

export function SessionWidget() {
  const router = useRouter();
  const { data: session, isPending } = useSession();

  if (isPending) return <p className="text-sm text-zinc-500">…</p>;

  if (!session) {
    return (
      <div className="flex gap-2">
        <Link href="/login">
          <Button variant="outline">Sign in</Button>
        </Link>
        <Link href="/signup">
          <Button>Sign up</Button>
        </Link>
      </div>
    );
  }

  return (
    <div className="flex items-center gap-3">
      <span className="text-sm">{session.user.email}</span>
      <Button
        variant="outline"
        onClick={async () => {
          await signOut();
          router.refresh();
        }}
      >
        Sign out
      </Button>
    </div>
  );
}
```

`apps/web/src/app/page.tsx` 전체 교체:

```tsx
import { SessionWidget } from "@/features/auth/session-widget";
import { api } from "@/lib/api";

async function getHealthStatus(): Promise<string> {
  try {
    const res = await api.health.$get();
    const body = await res.json();
    return body.status;
  } catch {
    return "unreachable";
  }
}

export default async function Home() {
  const status = await getHealthStatus();
  return (
    <main className="flex min-h-screen flex-col items-center justify-center gap-6">
      <p className="font-mono text-lg">
        baro — api status: <span className="font-bold">{status}</span>
      </p>
      <SessionWidget />
    </main>
  );
}
```

- [ ] **Step 7: 타입체크 + 수동 플로우 검증**

Run: `pnpm install && pnpm --filter @baro/web typecheck`
Expected: 클린. 특히 `api.api.auth.providers.$get()` 컴파일 = RPC 전파 확인.

Run: 루트 `pnpm dev` 백그라운드 후

```bash
# 가입
curl -s -c /tmp/baro-cookies.txt -X POST http://localhost:8000/api/auth/sign-up/email \
  -H 'content-type: application/json' \
  -d '{"name":"Manual Test","email":"manual@example.com","password":"password1234"}'
# 세션 확인
curl -s -b /tmp/baro-cookies.txt http://localhost:8000/api/auth/get-session
```

Expected: 두 응답 모두 `manual@example.com` 포함. 브라우저에서 `http://127.0.0.1:3000/login` 로그인 → 홈에서 이메일 표시 확인(주의: 이 머신은 `localhost:3000`에 다른 프로세스가 떠 있음 — `127.0.0.1` 사용). 확인 후 dev 종료.

- [ ] **Step 8: Commit**

```bash
git add apps/web pnpm-lock.yaml
git commit -m "feat(web): add auth pages, session widget and owned ui components"
```

---

### Task 5: 게이트 + CI 확인

**Files:**
- Modify: (필요 시) 이전 태스크 산출물의 lint 수정만

**Interfaces:**
- Consumes: 루트 `pnpm check`, Task 1-4 산출물
- Produces: main 푸시 + CI 그린

- [ ] **Step 1: 전체 게이트**

Run: `rtk proxy pnpm check`
Expected: 전 패키지 typecheck + test + biome 통과. 실패 시 수정 후 재실행 (수정 내용 보고).

- [ ] **Step 2: lockfile 정합 확인 (Plan 1 교훈)**

Run: `rtk proxy pnpm install --frozen-lockfile && git status --short`
Expected: install 성공 + 워킹트리 클린. 둘 중 하나라도 아니면 `pnpm install` 후 lockfile 커밋.

- [ ] **Step 3: Commit(잔여분) + Push + CI 확인**

```bash
git push
```

Run: 30초 간격 폴링

```bash
curl -s "https://api.github.com/repos/yg-cho/baro/actions/runs?per_page=1" | python3 -c "import json,sys; r=json.load(sys.stdin)['workflow_runs'][0]; print(r['status'], r['conclusion'])"
```

Expected: `completed success`. 실패 시 로그 확인 후 수정 커밋 반복.

---

## 완료 기준 (Plan 2)

- clone 상태에서 `pnpm dev` → `/signup` 가입 → `/login` 로그인 → 홈에 이메일 표시, DB 준비 별도 명령 0개 (PGlite 자동)
- `DATABASE_URL` 설정 시 Postgres로 동일 동작 (드라이버 스위치)
- 이메일 인증/재설정 링크가 api 콘솔에 출력
- 소셜 키 넣으면 로그인 버튼 자동 등장, 없으면 숨김
- `pnpm check` + CI 그린
- 다음 계획: Plan 3 관리자 대시보드 (admin 플러그인 — user 테이블 필드 이미 준비됨)

## 리스크 노트 (구현자·리뷰어 공용)

- better-auth 1.x API 표면은 버전에 따라 미세하게 다름 — 설치 버전 문서 기준으로 조정하고 모든 편차를 보고서에 기록
- packages/auth와 apps/web의 better-auth 버전 반드시 동일하게
- PGlite 동시 접근: api dev 서버와 vitest가 같은 dataDir을 열면 잠금 충돌 — 테스트는 `memory://` 강제 (vitest.config.ts env)
- `localhost:3000` 포트에 이 머신의 다른 프로세스 상주 — 브라우저 확인은 `127.0.0.1:3000`
